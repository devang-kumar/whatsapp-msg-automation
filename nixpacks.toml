[phases.setup]
nixPkgs = ["chromium", "chromedriver", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn -w 1 --threads 8 -b 0.0.0.0:$PORT app:app"
